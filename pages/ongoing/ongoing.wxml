<view wx:for="{{delivers}}" wx:key="unique">
  <deliver-card wx:key="unique" type="{{item.type}}" mobile="{{item.mobile}}" estate="{{item.estate}}" status="{{item.status}}" updateTime="{{updateTime}}" />
</view>