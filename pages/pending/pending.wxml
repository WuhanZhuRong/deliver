<view wx:for="{{delivers}}" wx:key="unique">
  <deliver-card type="{{item.type}}" mobile="{{item.mobile}}" estate="{{item.estate}}" status="{{item.status}}" name="{{item.name}}" updateTime="{{updateTime}}" />
</view>